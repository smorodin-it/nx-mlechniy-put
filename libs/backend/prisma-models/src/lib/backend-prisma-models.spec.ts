import { backendPrismaModels } from './backend-prisma-models';

describe('backendPrismaModels', () => {
  it('should work', () => {
    expect(backendPrismaModels()).toEqual('backend-prisma-models');
  });
});
